name: CI / Deploy to Hugging Face Space

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Avoid overlapping deploys if multiple pushes land quickly.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify (install, import, compile, test)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install runtime + dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Check required files exist
        run: |
          test -f app.py
          test -f requirements.txt
          test -f README.md

      - name: Compile all Python files (syntax check)
        run: python -m py_compile app.py $(find src tests -name "*.py")

      - name: Import the app (catches missing deps / import errors)
        run: python -c "import app"

      - name: Run unit tests
        run: pytest tests/ -v

  deploy:
    name: Deploy to Hugging Face Space
    needs: verify
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_USERNAME: seahli
          HF_SPACE_NAME: SoundAnalysis1
        run: |
          if [ -z "${HF_TOKEN}" ]; then
            echo "HF_TOKEN GitHub Actions secret is missing."
            exit 1
          fi
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          git push --force "https://${HF_USERNAME}:${HF_TOKEN}@huggingface.co/spaces/${HF_USERNAME}/${HF_SPACE_NAME}" HEAD:main
